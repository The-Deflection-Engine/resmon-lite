#!/bin/sh
# Run resmon-lite from this checkout without installing it.
DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec env PYTHONPATH="$DIR${PYTHONPATH:+:$PYTHONPATH}" python3 -m resmon_lite "$@"
